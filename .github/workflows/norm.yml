name: check norminette v3
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-10.15
    
    steps:
      - uses: actions/checkout@v2
      - name: norminette
      - uses: alexandregv/norminette-action@v3
        with:
          flags: '.'
